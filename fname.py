# encoding: utf-8
import os
import shutil
import argparse
from inc import EPILOG

def ParseCommandLine():
    parser = argparse.ArgumentParser(
        description="文件重命名",
        epilog=EPILOG,
        )
    parser.add_argument(
        "", "",
    )